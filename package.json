{
  "name": "kennel",
  "version": "0.1.0",
  "description": "Open-source support desk and incident console for fleets of AI agents",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "start": "node src/api/server.ts",
    "worker": "node src/worker/index.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
