# Contributing

Tests first. `npm test` must be green and `npm run typecheck` clean before a PR. Read
`docs/architecture.md`; a PR that weakens an invariant needs an ADR that replaces it.
Commit messages: conventional (`feat:`, `fix:`, `docs:`, `test:`). No secrets, ever.
